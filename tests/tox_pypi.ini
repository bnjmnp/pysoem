[tox]
requires =
    tox>=4
env_list = py{37,38,39,310,311,312,313,314}

[testenv]
description = test a freshly downloaded pysoem from PyPI
deps =
    pytest
    pysoem
commands =
    pytest {posargs}